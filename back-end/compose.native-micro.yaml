services:
  back-end:
    build:
      context: .
      dockerfile: ./src/main/docker/Dockerfile.native-micro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/hello"]
      interval: 5s
      timeout: 5s
      retries: 5
    extra_hosts:
      - host.docker.internal:host-gateway
